export type userAnalyticsType = {
    user_id: string,
    views: number,
    uploads: number,
    sales: number,
    earning: number
}

export type getUserAnalyticsType = {
    page: number,
    limit: number
}